import {
  aboutpage,
  appointmentpage,
  blogpage,
  contactpage,
  homepage,
  regularpage,
  servicepage,
} from "./types/pages.collection";
import {
  ctaSection,
  faqsSection,
  pricingSection,
  testimonialSection,
} from "./types/sections.collection";

// Export collections
export const collections = {
  // Pages
  homepage,
  blogpage,
  aboutpage,
  servicepage,
  appointmentpage,
  contactpage,
  regularpage,

  // sections
  ctaSection,
  testimonialSection,
  faqsSection,
  pricingSection,
};
