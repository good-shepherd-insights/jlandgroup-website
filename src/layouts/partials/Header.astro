---
import config from "@/config/config.json";
import menu from "@/config/menu.json";

export interface ChildNavigationLink {
  name: string;
  url: string;
}

export interface NavigationLink {
  name: string;
  url: string;
  hasChildren?: boolean;
  children?: ChildNavigationLink[];
}

const { main }: { main: NavigationLink[] } = menu;
const { pathname } = Astro.url;
---

<style>
  /* Brand first, navigation underneath. Daniel, 2026-08-26: the logo belongs at
     the very top and all the tabs below it. */
  /* The theme floats .header over the hero (its script adds Tailwind's
     `absolute` at scroll < 250). That suits a transparent bar; this is a solid
     brand band 300px tall, and overlaying it swallowed the hero headline.
     Keep it in normal flow. */
  .masthead {
    position: relative !important;
    background: #0e0f12;
    border-bottom: 1px solid rgba(255, 255, 255, .08);
  }

  .masthead__brand { display: flex; justify-content: center; padding: 2rem 1rem 1.35rem; }
  .masthead__logo {
    display: block; height: auto;
    filter: drop-shadow(0 3px 16px rgba(0,0,0,.7));
    width: min(clamp(300px, 44vw, 620px), 88vw);
  }

  .masthead__bar {
    position: relative;
    display: flex; align-items: center; justify-content: center;
    gap: 1rem; padding: 0 1rem 1rem;
  }

  .masthead__nav { display: flex; align-items: center; justify-content: center; gap: .35rem 1.15rem; }
  .masthead__nav :global(.nav-link) {
    color: rgba(255,255,255,.86);
    font-size: clamp(1rem, 1.15vw, 1.125rem);
    font-weight: 500;
    letter-spacing: .085em;
    text-transform: uppercase;
    padding: .55rem .5rem;
    transition: color .18s ease;
  }
  .masthead__nav :global(.nav-link:hover),
  .masthead__nav :global(.nav-link.active) { color: #fff; }
  .masthead__nav :global(.nav-dropdown-list) { background: #14161a; }
  .masthead__nav :global(.nav-dropdown-link) { color: rgba(255,255,255,.82); }
  .masthead__nav :global(.nav-dropdown-link:hover) { color: #fff; }

  /* Mobile: logo stays centred, tabs collapse behind the toggle. */
  .masthead__toggle { display: none; color: #fff; }
  @media (max-width: 1023px) {
    .masthead__brand { padding: 1.35rem 1rem .95rem; }
    .masthead__bar { justify-content: flex-end; padding: 0 1.25rem .9rem; }
    .masthead__toggle { display: inline-flex; cursor: pointer; align-items: center; }
    .masthead__nav { display: none; width: 100%; flex-direction: column; align-items: stretch; gap: 0; }
    #nav-toggle:checked ~ .masthead__nav { display: flex; padding-bottom: .75rem; }
  }
</style>

<header class="masthead header z-30 w-full">
  <div class="masthead__brand">
    <a href="/" aria-label="JLand Group — home">
      <img
        class="masthead__logo"
        src="/images/logo-jland-group-v5.png"
        alt="JLand Group — Contracting &amp; Restoration"
        width="1982" height="490"
        fetchpriority="high"
      />
    </a>
  </div>

  <div class="masthead__bar container">
    <input id="nav-toggle" type="checkbox" class="hidden" />
    <label for="nav-toggle" class="masthead__toggle" aria-label="Menu">
      <svg id="show-button" class="h-6 fill-current block" viewBox="0 0 20 20">
        <title>Menu Open</title>
        <path d="M0 3h20v2H0V3z m0 6h20v2H0V9z m0 6h20v2H0V0z"></path>
      </svg>
      <svg id="hide-button" class="h-6 fill-current hidden" viewBox="0 0 20 20">
        <title>Menu Close</title>
        <polygon
          points="11 9 22 9 22 11 11 11 11 22 9 22 9 11 -2 11 -2 9 9 9 9 -2 11 -2"
          transform="rotate(45 10 10)"></polygon>
      </svg>
    </label>

    <ul id="nav-menu" class="masthead__nav navbar-nav">
      {
        main.map((menu) => (
          <>
            {menu.hasChildren ? (
              <li class="nav-item nav-dropdown group relative">
                <input type="checkbox" id={`submenu-${menu.name}`} class="peer hidden lg:hidden" />
                <label
                  for={`submenu-${menu.name}`}
                  class={`nav-link inline-flex items-center ${
                    menu.children?.map(({ url }) => url).includes(pathname) ||
                    menu.children?.map(({ url }) => `${url}/`).includes(pathname)
                      ? "active"
                      : ""
                  }`}
                >
                  {menu.name}
                  <svg class="h-4 w-4 fill-current" viewBox="0 0 20 20">
                    <path d="M9.293 12.95l.707.707L15.657 8l-1.414-1.414L10 10.828 5.757 6.586 4.343 8z" />
                  </svg>
                </label>
                <ul class="nav-dropdown-list hidden peer-checked:block lg:invisible lg:absolute lg:block lg:opacity-0 lg:group-hover:visible lg:group-hover:opacity-100">
                  {menu.children?.map((child) => (
                    <li class="nav-dropdown-item">
                      <a
                        href={child.url}
                        aria-label={child.name}
                        class={`nav-dropdown-link block ${
                          (pathname === `${child.url}/` || pathname === child.url) && "active"
                        }`}
                      >
                        {child.name}
                      </a>
                    </li>
                  ))}
                </ul>
              </li>
            ) : (
              <li class="nav-item">
                <a
                  href={menu.url}
                  class={`nav-link block ${
                    (pathname === `${menu.url}/` || pathname === menu.url) && "active"
                  }`}
                >
                  {menu.name}
                </a>
              </li>
            )}
          </>
        ))
      }
    </ul>

  </div>
</header>
